node_modules
.next
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging
.DS_Store
*.log
.vercel
next-env.d.ts
tsconfig.tsbuildinfo
# Internal planning / brand / process docs — not tracked in git.
# Every file under docs/ (implementation logs, brand guidelines, plans)
# stays local. Move future "notes" files here to keep them out of the
# repository automatically.
docs/
certificates